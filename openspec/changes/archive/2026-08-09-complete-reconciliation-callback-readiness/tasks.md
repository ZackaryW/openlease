## 1. Feature Contract

- [x] 1.1 Add version-four callback-input and repository-isolated cohort scenarios to the affected BDD feature roots.
- [x] 1.2 Map the affected feature roots and public surfaces through the existing behavior configuration.

## 2. Public Callback Contract

- [x] 2.1 Advance the extension contract to version four and make callback selection capture validated immutable optional managed input.
- [x] 2.2 Cover callback input validation, immutability, typing, and version rejection with focused unit tests.

## 3. Reconciliation Dispatch

- [x] 3.1 Include captured callback input in reconciliation plans and callback drift evidence.
- [x] 3.2 Pass the captured input into repository callbacks and bind cohort callbacks once per completed repository in deterministic repository-major order.
- [x] 3.3 Preserve repository-specific event identity and observational outcome reporting across failures.

## 4. Verification and Documentation

- [x] 4.1 Wire BDD bindings for explicit ZPP-like input, plan evidence, immutable capture, isolated cohort contexts, ordering, and continued observational dispatch.
- [x] 4.2 Update public extension documentation for version four and the callback input/fan-out migration.
- [x] 4.3 Run focused tests, complete mapped BDD verification, the supported Python matrix, lint, and strict OpenSpec validation.
